const BetterSet = require('../src/BetterSet.js')

// Features to build:
// 1. add
// 2. contains
// 3. size
// 4. uniqueness
// 5. remove

describe('BetterSet', () => {
  it('can be tested', () => {
    expect(5).toEqual(5)
  })
})
